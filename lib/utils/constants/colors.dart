import 'package:flutter/material.dart';

class UPColors {
  UPColors._();

  // Brand
  static const Color primary = Color(0xFF684AC0);
  static const Color primaryDark = Color(0xFF1A1033);

  // Sent — your primary blue, slightly deeper for readability
  static const Color bubbleSent = Color(0xFFDFD5FF);

  // Received — warm off-white, not cold grey
  static const Color bubbleReceived = Color(0xFFEBEBEB);

  // Theme Colors
  static const Color light = Color(0xFFF6F6F6);
  static const Color dark = Color(0xFF272727);

  // Background
  static const Color fieldBackground = Color(0xFFD9E2E4);

  // Text
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textWhite = Color(0xFFFFFFFF);

  //  Status
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF684AC0);

  // Message ticks
  static const Color tickSent = Color(0xFF475569);
  static const Color tickRead = Color(0xFF008ADE);

  // Online presence
  static const Color onlineGreen = Color(0xFF4CAF50);

  // Neutrals
  static const Color black = Color(0xFF0F172A);
  static const Color darkGrey = Color(0xFF475569);
  static const Color darkerGrey = Color(0xFF4F4F4F);
  static const Color grey = Color(0xFF94A3B8);
  static const Color lightGrey = Color(0xFFE2E8F0);
  static const Color white = Color(0xFFFFFFFF);

  // Dark gradient background
  static const Color gradientTop = Color(0xFF0D0D1A);
  static const Color gradientMiddle = Color(0xFF1A1033);
  static const Color gradientBottom = Color(0xFF0D1829);

  // Light mode gradient background
  static const Color gradientLightTop = Color(0xFF7C5CBF);
  static const Color gradientLightMiddle = Color(0xFF9B7DD4);
  static const Color gradientLightBottom = Color(0xFF6B4FAF);
}
